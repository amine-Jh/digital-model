-- =============================================================================
-- Template for incremental migrations (CogniTest)
-- Location: supabase/migrations/
-- =============================================================================
-- 1. Copy this file to the next free name, e.g.  0006_add_foo_column.sql
--    (zero-padded number + underscore + short_description.sql).
-- 2. Replace the placeholder below with real DDL/DML.
-- 3. Prefer idempotent patterns (IF NOT EXISTS, DROP POLICY IF EXISTS, …).
-- 4. Run after 0001_baseline.sql and all lower-numbered migrations on each environment.
--    See supabase/migrations/README.md.
-- =============================================================================

-- Example: add a nullable column
-- alter table public.some_table
--   add column if not exists new_field text;

-- Example: new RLS policy
-- drop policy if exists my_policy on public.some_table;
-- create policy my_policy on public.some_table
--   for select using (auth.uid() is not null);

select 1 as migrations_template_placeholder;
